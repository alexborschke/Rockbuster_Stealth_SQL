#Using the COUNT and DISTINCT function, calculate the number of unique film_ids;
#Using the AVG function, calculate the averages of the following columns: 'rental_rate', 'rental_duration', 'relacement_cost';
#Using AS to create aliases for the calculation results. 

SELECT
COUNT(DISTINCT film_id) AS number_of_films,
AVG(rental_rate) AS average_rental_rate,
AVG(rental_duration) AS avg_rental_duration,
AVG(replacement_cost) AS avg_replacement_cost
FROM film
